git status

git init

dotnet new -l

dotnet new gitignore 

// change
 git remote set-url origin https://github.com/Tastyfishnomnomnom/React-DotnetEshop.git

 //Normal
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Tastyfishnomnomnom/React-DotnetEshop.git
git push -u origin main


//remove origin
git remote rm origin



 git remote show origin

>$ git remote rm origin

>$ git add .

>$ git commit -m "First commit"

>$ git remote add origin Copied_origin_url

>$ git remote show origin

>$ git push origin master